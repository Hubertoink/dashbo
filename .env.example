# Shared env (docker compose reads this)

# OpenWeatherMap
OWM_API_KEY=
OWM_LAT=49.4875
OWM_LON=8.4660
OWM_UNITS=metric
OWM_LANG=de

# Backend
BACKEND_PORT=3000
CORS_ORIGIN=http://localhost:8080
JWT_SECRET=change-me
BOOTSTRAP_ADMIN_EMAIL=admin@example.com
BOOTSTRAP_ADMIN_PASSWORD=change-me
BOOTSTRAP_ADMIN_NAME=Admin

# Outlook Kalender (privat, pro User, nur anzeigen)
# Redirect URI (docker/nginx): http://localhost:8080/api/outlook/callback
OUTLOOK_TENANT=consumers
OUTLOOK_CLIENT_ID=
OUTLOOK_CLIENT_SECRET=
OUTLOOK_REDIRECT_URI=http://localhost:8080/api/outlook/callback
OUTLOOK_SCOPES=offline_access Calendars.Read User.Read

# Postgres
POSTGRES_DB=dashbo
POSTGRES_USER=dashbo
POSTGRES_PASSWORD=dashbo
POSTGRES_PORT=5432

# Outlook Kalender (privat, pro User, nur anzeigen)
# Azure App Registration (Microsoft Identity Platform)
# Redirect URI (für Docker + nginx): http://localhost:8080/api/outlook/callback
OUTLOOK_TENANT=consumers
OUTLOOK_CLIENT_ID=
OUTLOOK_CLIENT_SECRET=
OUTLOOK_REDIRECT_URI=http://localhost:8080/api/outlook/callback
OUTLOOK_SCOPES=offline_access Calendars.Read User.Read
